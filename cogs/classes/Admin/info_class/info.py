import discord
from asyncinit import asyncinit
from typing import Optional, List, Dict, Any
from dataclasses import field, fields, dataclass


discord
    

class Main:
    def __init__(self, ctx, opt):
        self.data = Opt(ctx, opt)

AA = ["a", "b"]
    
A = Main(*AA)
